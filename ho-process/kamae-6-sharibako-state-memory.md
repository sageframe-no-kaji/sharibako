---
created: 2026-07-09
type: state-memory
project: sharibako
kamae: 6
status: living
---

# Sharibako — State Memory (Kamae 6)

This file is the build's living cross-session memory. It is hot and non-canonical: mutable, written raw, and always subordinate to the cold canonical record (git history, per-ho Reflect sections, the K4 ho overview). When this file and the cold record disagree, the cold record wins and this file is corrected to match.

---

**STATE-SUMMARY**
- **COMPLETED** — **ho-06.7 (Delete: the missing destructive verb) EXECUTED, GATED, CLOSED, MERGED 2026-07-12** — gate passed on the signed `/Applications` build (rust reads right on both destructive buttons, system dialogs acceptable, deletion feels safe). **PR #16 merged to `main` (`9afadef`); `delete-verb` and `panel-chrome` branches deleted.** ho-06.5 (PR #15) also merged this day. Working branch back on `main`, clean. **THREE verbs folded in (scope + shared + per-key — the last widened mid-build after dogfooding).** Core (in `VaultCore+Deletion.swift`, split out for the body-length ceiling): `deleteScope(_:)`, `deleteSharedEntry(_:force:)`, `deleteSecret(_:inScope:)` — all on the keyless `VaultCore(vaultURL:)`, filesystem-only, no age/no Touch ID; shared-delete guarded by `linkGraph` (new `VaultError.sharedEntryLinked(id:linkers:)`), `--force`/`force:` orphans the linkers (left dangling for heal/orphan); `deleteSecret` removes the key's `.age` or `.link` (a link-delete drops only the pointer). CLI: `sharibako delete <scope>` / `<scope> <key>` / `--shared` / `--yes` / `--force`, confirmation like `clean` (TTY-gated, exit 130 on decline), ErrorReporter names the linkers + unlink-or-force hint, `--json` success. GUI: rust-toned (`Color.drift`) **Delete Scope** button in the ActionPanel scope group AND **Delete Secret** in the detail pane — **destructive-rust's FIRST real in-window consumers, closing the 06.4/06.5 item**; each stages a pending deletion (`pendingScopeDeletion`/`pendingSecretDeletion`) → **system-rendered** confirm dialog (dialogs untintable, per the 06.5 gate) → keyless confirm intent (clears selection, reloads, announces). **GUI delete-SHARED still deferred to ho-07** (no shared-entry surface in the Workshop yet); Core + CLI carry it now. Blast radius = vault only (markers + materialized `.env` left in place, git retains/recoverable, `sync` commits — not auto). 759 tests (+28: 11 Core, 9 CLI, 8 GUI-model), warnings-as-errors + both linters clean; ActionPanel/WorkshopWindow/SecretDetail stay CI-excluded views. **K4 pass done** (06.5 marked closed/PR #15, 06.7 registered, tail resequenced delete → first-run → multi-root). Prior: ho-06.5 CLOSED 2026-07-11 (panel + flat grounds, **PR #15 — MERGED 2026-07-12**).
- **NEXT** — **ho-06.3** (first-run wizard + age-key gen + backup nudge + GUI ingest — the Checkpoint-2 gate; the ho the 15-minute first-version criterion actually tests). Branch it fresh off `main` (both 06.5 and 06.7 are merged — no stacking). Open decisions to resolve in its Think phase: backup-nudge UX (Deferred Decision #2), full first-run flow (Deferred Decision #9), vault-location default, default scan-root suggestion. Premise gate-validated at 06.1: CLI `init` is still the only repos-to-vault front door.
- **ACTION ITEMS / BLOCKS** — none blocking. Branch topology is clean: PRs #15 and #16 are both merged to `main`; `panel-chrome` and `delete-verb` are deleted; new work branches fresh off `main`. Still owed: **ho-06.3** (the NEXT), **ho-06.6** (multi-root scan management — panel is its home), the CLI ho (scriptable `init`, `git init`, non-atomic `ingest`), unlinked-markers rows UI-unverified (need a gate with a stray `.sharibako`), the "plain/not-secret" flag schema Think (parked, Kamae-2). Noted, unscheduled: a custom in-window confirmation surface if system dialogs keep rankling. `ScopeType` confirmed tag-only. **GUI build/dogfood path:** committed `xcode/Sharibako.xcodeproj` → signed `Sharibako.app` in `/Applications`, launched from there explicitly (NOT `swift run`, NOT `install.sh`; Spotlight resurfaces stale DerivedData builds).
- **PROJECT LIFECYCLE** — `dev`

_Updated 2026-07-16: corrected to the cold record — PR #16 (delete-verb) and PR #15 (panel-chrome) are both MERGED (2026-07-12); the stale stacked-branch note and "push/PR pending" trailer removed. Previous entry: ho-06.7 close (2026-07-12). Before that: ho-06.5 close (PR #15); ho-06.4 merged (PR #14, `60f78f2`); ho-06.2 close (PR #13); ho-06.1 (merged 2026-07-10)._
