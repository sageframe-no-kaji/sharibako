.\" "Generated by swift-argument-parser"
.Dd July 8, 2026
.Dt SHARIBAKO.INIT 1
.Os
.Sh NAME
.Nm "sharibako init"
.Nd "Initialize a project directory as a sharibako scope."
.Sh SYNOPSIS
.Nm
.Op Fl -vault Ar vault
.Op Fl -age-key Ar age-key
.Op Fl -json
.Op Fl -verbose
.Op Ar directory
.Op Fl -no-generate
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
Reads the .env (and .env.local) in a project directory and walks each detected secret through an interactive per-key decision: import as scope-local, link to an existing shared entry, move to shared, leave alone, or skip. The chosen secrets are encrypted into the vault and a .sharibako marker is dropped in the directory, binding it to its scope and recording the materialize target. The source .env is left byte-for-byte untouched - it is the runtime artifact, not a duplicate to clean up.

\&'init' is interactive by design and requires a terminal. If no age key exists yet it offers to generate one inline (suppress with --no-generate). Re-running 'init' in an already-initialized directory reconciles: it presents only keys the scope does not already own, and never silently rebinds the directory to a different scope. A directory with no importable secrets is rejected rather than creating an empty scope.

\&'init' is the front door for an existing project; to add a single key to a scope later, use 'sharibako add'.

EXAMPLES

Initialize the current project, deciding each secret interactively:
  sharibako init

Initialize a specific directory without offering key generation:
  sharibako init ~/Projects/momiji --no-generate

EXIT CODES

Exits 2 when the directory has no secrets to import or when a prompt is declined; needs an interactive terminal.
.Bl -tag -width 6n
.It Fl -vault Ar vault
Override the vault directory path.
.It Fl -age-key Ar age-key
Use this age key file instead of the Keychain.
.It Fl -json
Output in JSON format.
.It Fl v , -verbose
Enable verbose logging.
.It Ar directory
Project directory to initialize (defaults to the current directory).
.It Fl -no-generate
Do not offer to generate an age key if none exists (fail instead).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std
.Sh AUTHORS
The
.Nm
reference was written by
.An -nosplit
.An "Andrew Marcus"
.Ao
.Mt andrew@sageframe.net
.Ac .
.Sh SEE ALSO
.Xr sharibako 1 ,
.Xr sharibako.add 1 ,
.Xr sharibako.clean 1 ,
.Xr sharibako.get 1 ,
.Xr sharibako.heal 1 ,
.Xr sharibako.key 1 ,
.Xr sharibako.key.export 1 ,
.Xr sharibako.key.generate 1 ,
.Xr sharibako.key.import 1 ,
.Xr sharibako.link 1 ,
.Xr sharibako.list 1 ,
.Xr sharibako.materialize 1 ,
.Xr sharibako.rotate 1 ,
.Xr sharibako.run 1 ,
.Xr sharibako.scan 1 ,
.Xr sharibako.status 1 ,
.Xr sharibako.sync 1 ,
.Xr sharibako.unlink 1 ,
.Xr sharibako.update 1
.Sh REPORTING BUGS
Report bugs, or security issues privately per SECURITY.md, at
.Lk https://github.com/sageframe-no-kaji/sharibako/issues .
