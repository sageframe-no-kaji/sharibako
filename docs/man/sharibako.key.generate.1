.\" "Generated by swift-argument-parser"
.Dd July 8, 2026
.Dt SHARIBAKO.KEY.GENERATE 1
.Os
.Sh NAME
.Nm "sharibako key generate"
.Nd "Generate a new age key pair."
.Sh SYNOPSIS
.Nm
.Op Fl -vault Ar vault
.Op Fl -age-key Ar age-key
.Op Fl -json
.Op Fl -verbose
.Op Fl -force
.Op Fl -yes
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
Generates a fresh age key pair and stores the private key: in the macOS Keychain (Touch ID gated) by default, or to a file when --age-key <path> is given (or on Linux). The public recipient key is printed - save it somewhere durable off the machine, because it is the string you back up and the vault cannot be recovered without the private key. This is also the bootstrap command on a fresh install: it creates the vault directory alongside the key.

\&'generate' refuses to clobber an existing key unless --force is given, and even then confirms interactively unless --yes is also passed - replacing the key that decrypts your vault makes every existing secret unreadable, so the two-flag gate is deliberate. Generation is staged and swapped into place atomically: if key creation fails, the existing key (the only thing that decrypts the vault) survives untouched.

EXAMPLES

First-time bootstrap (Keychain on macOS):
  sharibako key generate

Generate a file-based key for Linux or CI:
  sharibako key generate --age-key ~/.config/sharibako/age-key

Deliberately replace an existing key, no prompt:
  sharibako key generate --force --yes

EXIT CODES

Exits 2 when a key already exists and --force was not given.
.Bl -tag -width 6n
.It Fl -vault Ar vault
Override the vault directory path.
.It Fl -age-key Ar age-key
Use this age key file instead of the Keychain.
.It Fl -json
Output in JSON format.
.It Fl v , -verbose
Enable verbose logging.
.It Fl -force
Replace an existing age key (refuses without it, exit 2). Destroys access to the current vault.
.It Fl -yes
Skip the replacement confirmation prompt (used with --force).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std
.Sh AUTHORS
The
.Nm
reference was written by
.An -nosplit
.An "Andrew Marcus"
.Ao
.Mt andrew@sageframe.net
.Ac .
.Sh SEE ALSO
.Xr sharibako 1 ,
.Xr sharibako.add 1 ,
.Xr sharibako.clean 1 ,
.Xr sharibako.get 1 ,
.Xr sharibako.heal 1 ,
.Xr sharibako.init 1 ,
.Xr sharibako.key 1 ,
.Xr sharibako.key.export 1 ,
.Xr sharibako.key.import 1 ,
.Xr sharibako.link 1 ,
.Xr sharibako.list 1 ,
.Xr sharibako.materialize 1 ,
.Xr sharibako.rotate 1 ,
.Xr sharibako.run 1 ,
.Xr sharibako.scan 1 ,
.Xr sharibako.status 1 ,
.Xr sharibako.sync 1 ,
.Xr sharibako.unlink 1 ,
.Xr sharibako.update 1
.Sh REPORTING BUGS
Report bugs, or security issues privately per SECURITY.md, at
.Lk https://github.com/sageframe-no-kaji/sharibako/issues .
