.\" "Generated by swift-argument-parser"
.Dd July 8, 2026
.Dt SHARIBAKO.ADD 1
.Os
.Sh NAME
.Nm "sharibako add"
.Nd "Encrypt and add a new secret to a scope."
.Sh SYNOPSIS
.Nm
.Op Fl -vault Ar vault
.Op Fl -age-key Ar age-key
.Op Fl -json
.Op Fl -verbose
.Ar scope
.Ar key
.Op Fl -value Ar value
.Op Fl -from-stdin
.Op Fl -notes Ar notes
.Op Fl -force
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
Encrypts a value and stores it as a new scope-local secret (<KEY>.age) under the given scope. Touch ID fires once to unlock the age key. 'add' is for a NEW key: it refuses to overwrite an existing one unless --force is given - use 'sharibako rotate' to change the value of a key that already exists, and 'sharibako link' to point a key at a shared entry instead of storing its own value.

HOW THE VALUE ENTERS

The value can arrive three ways, with different exposure. With neither --value nor --from-stdin on a terminal, Sharibako prompts with input hidden (nothing lands in shell history or on screen) - the hygienic default. --from-stdin reads the value from a pipe, exposing only whatever produced the pipe. --value <v> puts the secret on your command line, where it lands in shell history and is visible in 'ps' for the duration of the run; use it in scripts that already hold the value, not interactively.

EXAMPLES

Add a project-local secret, prompted with hidden input:
  sharibako add kanyo-dev DATABASE_URL

Pipe a value in from another tool, nothing in argv or history:
  op read "op://Personal/OpenAI/key" | sharibako add kanyo-dev OPENAI_API_KEY --from-stdin

Overwrite an existing key deliberately, with a note:
  sharibako add kanyo-dev API_TOKEN --value "..." --force --notes "rotated by ops"

EXIT CODES

Exits 2 when the key already exists without --force, 4 on an age encryption failure. See sharibako(1) for the full taxonomy.
.Bl -tag -width 6n
.It Fl -vault Ar vault
Override the vault directory path.
.It Fl -age-key Ar age-key
Use this age key file instead of the Keychain.
.It Fl -json
Output in JSON format.
.It Fl v , -verbose
Enable verbose logging.
.It Ar scope
Scope to add the secret to.
.It Ar key
Secret key name (e.g. DATABASE_URL).
.It Fl -value Ar value
Plaintext value. Prefer the hidden prompt or --from-stdin; --value leaks into shell history and 'ps'.
.It Fl -from-stdin
Read the value from stdin (e.g. piped from another tool).
.It Fl -notes Ar notes
Optional notes stored (encrypted) alongside the value.
.It Fl -force
Overwrite an existing key (add otherwise refuses, exit 2).
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std
.Sh AUTHORS
The
.Nm
reference was written by
.An -nosplit
.An "Andrew Marcus"
.Ao
.Mt andrew@sageframe.net
.Ac .
.Sh SEE ALSO
.Xr sharibako 1 ,
.Xr sharibako.clean 1 ,
.Xr sharibako.get 1 ,
.Xr sharibako.heal 1 ,
.Xr sharibako.init 1 ,
.Xr sharibako.key 1 ,
.Xr sharibako.key.export 1 ,
.Xr sharibako.key.generate 1 ,
.Xr sharibako.key.import 1 ,
.Xr sharibako.link 1 ,
.Xr sharibako.list 1 ,
.Xr sharibako.materialize 1 ,
.Xr sharibako.rotate 1 ,
.Xr sharibako.run 1 ,
.Xr sharibako.scan 1 ,
.Xr sharibako.status 1 ,
.Xr sharibako.sync 1 ,
.Xr sharibako.unlink 1 ,
.Xr sharibako.update 1
.Sh REPORTING BUGS
Report bugs, or security issues privately per SECURITY.md, at
.Lk https://github.com/sageframe-no-kaji/sharibako/issues .
